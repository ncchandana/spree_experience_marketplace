<div id="experience-related-products" class="col-md-12">
<div class="section-title">
	<% unless @product.experiences.empty? %>
		<h1><%= Spree.t(:more_products_by) %> <%= @product.experiences.first.name %></h1>
	</div>
	<%= render :partial => 'spree/shared/products', :locals => { :products => @product.experiences.first.products[0..3] } %>
	<% end %>
</div>
